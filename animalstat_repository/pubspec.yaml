name: animalstat_repository

version: 1.0.0

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  pedantic: ^1.8.0
  equatable: ^1.2.5

  # Add Firebase plugins
  firebase_core: ^0.7.0
  cloud_firestore: ^0.16.0
  firebase_auth: ^0.20.0+1